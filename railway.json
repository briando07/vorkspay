{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx vite build",
    "rootDir": "frontend"
  },
  "deploy": {
    "startCommand": "npx vite preview --host",
    "rootDir": "frontend"
  }
}
