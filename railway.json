{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "deploy": {
    "startCommand": "npm run preview --prefix frontend"
  }
}
