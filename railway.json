{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd frontend && npm ci && npm install vite && npx vite build"
  },
  "deploy": {
    "startCommand": "cd frontend && npx vite preview --host"
  }
}
